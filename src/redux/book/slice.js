import { createSlice } from '@reduxjs/toolkit'

const initialState = {}

const bookSlice = createSlice({
	name: 'book',
	initialState,
	reducers: {},
	extraReducers: {},
})

export default bookSlice.reducer
